# Default relay configuration — override in .env.local (gitignored)
VITE_RELAY_HOST=localhost
VITE_RELAY_WS_PORT=9001
VITE_RELAY_INFO_PORT=9002
# Set to "true" to route relay WebSocket through the dev proxy (WSS)
VITE_RELAY_WSS=false
VITE_PROXY_PORT=8443
